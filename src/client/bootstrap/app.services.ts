console.log('app.services');

// import container from '@services/service.container';
// import ServiceIdentifiers from '@services/service.identifiers';
//
// const services = {
//   ...Object.values(ServiceIdentifiers).reduce((aggregate, symbol) => ({ ...aggregate, [symbol]: container.get(symbol) }), {})
// };
//
// export default services;
